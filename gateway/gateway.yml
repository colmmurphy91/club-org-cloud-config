server:
  port: 8080
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
#       routes:
#         - id: core-team-service
#           uri: lb://core-team-service
#           predicates:
#             - Path=/teamservice/**
